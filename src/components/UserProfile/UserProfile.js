import Header from "../Header/Header";

const UserProfile = () => {
    return (
        <div>
            <Header title="Профиль пользователя" />
            UserProfile
        </div>
    );
};

export default UserProfile;
